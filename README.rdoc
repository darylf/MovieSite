= Movie Site

This is a test app I'm using to figure out +has_many :through+

I haven't written any tests, as this is simply a sandbox/demo.

== Run

To attempt this demo, run the following codes

  $ rake db:migrate
  $ rake db:seed
  $ rails s
